from django.apps import AppConfig

class EvaluatorConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'evaluator'
    verbose_name = 'Evaluador de Hojas de Vida ANEIAP'
